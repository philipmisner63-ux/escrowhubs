export * from "./registry";
// Legacy ChainConfig helpers — for components that use the full chain config shape
export { getChain, getDefaultChain, chainRegistry, type ChainConfig } from "@/lib/chainRegistry";
